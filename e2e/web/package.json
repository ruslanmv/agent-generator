{
  "name": "@agent-generator/e2e-web",
  "private": true,
  "version": "0.1.0",
  "description": "Playwright smoke flows for the Agent Generator web shell (and, by extension, the Tauri webview — same React SPA).",
  "type": "module",
  "scripts": {
    "install:browsers": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.3"
  }
}
