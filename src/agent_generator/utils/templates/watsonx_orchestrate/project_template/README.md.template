# {project_name}

This is a **watsonx_orchestrate** project generated by the **Agent-Generator**.

## 🚀 Getting Started

### 1. Environment Setup
First, configure your environment variables. Copy the template file:
```bash
cp .env.template .env
```
Now, edit the `.env` file and fill in your specific `WO_INSTANCE` and `WO_API_KEY`.

### 2. Install Dependencies
This project uses a Python virtual environment to manage dependencies. To install them, simply run:
```bash
make install
```

### 3. Run and Deploy
To execute the main deployment script (`scripts/deploy.sh`), use the following command:
```bash
make run
```

## 📊 Evaluations
To run the evaluation scripts, use:
```bash
make evaluate
```

## 🗑️ Cleanup
To remove the virtual environment and any generated results, run:
```bash
make clean
```
