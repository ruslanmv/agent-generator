# {project_name}

This is a **watsonx_orchestrate_mcp** project generated by the **Agent-Generator**.

## 🚀 Getting Started

### 1. Environment Setup
First, configure your environment variables. Copy the template file:
```bash
cp .env.template .env
```
Now, edit the `.env` file and fill in your specific `WO_INSTANCE` and `WO_API_KEY`.

### 2. Install Dependencies
This project uses a Python virtual environment to manage all dependencies, including those for the MCP tool. To install them, simply run:
```bash
make install
```

### 3. Run the MCP Server (for local development)
To test your MCP tool locally, you can run its server:
```bash
make run-mcp-server
```

### 4. Import All Assets
To import all agents and toolkits defined in this project into your watsonx Orchestrate instance, run:
```bash
make import-all
```
This will execute the `scripts/import-all.sh` script.

## 🗑️ Cleanup
To remove the virtual environment and any generated results, run:
```bash
make clean
```
