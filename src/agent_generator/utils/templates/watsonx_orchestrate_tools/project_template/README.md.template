# {project_name}

This is a **watsonx_orchestrate_tools** project generated by the **Agent-Generator**.

## 🚀 Getting Started

### 1. Environment Setup
First, configure your environment variables. Copy the template file:
```bash
cp .env.template .env
```
Now, edit the `.env` file and fill in your specific `WO_INSTANCE` and `WO_API_KEY`.

### 2. Install Dependencies
This project uses a Python virtual environment to manage dependencies. To install them, simply run:
```bash
make install
```

### 3. Import All Assets
To import all agents and tools defined in this project into your watsonx Orchestrate instance, run:
```bash
make import-all
```
This will execute the `scripts/import-all.sh` script.

## 🗑️ Cleanup
To remove the virtual environment and any generated results, run:
```bash
make clean
```
